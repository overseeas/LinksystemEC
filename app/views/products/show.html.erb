<h1><%= @product.code %></h1>

<ul>
    <li><%= link_to "Edit", edit_product_path(@product) %></li>
    <li>
        <%= link_to "Destroy", product_path(@product), data:{
            turbo_method: :delete,
            turbo_confirm: "Are you sure?"
        } %>
    </li>
</ul>

<h2>Rakuten info</h2>
<%= render @product.rakuten_products %>

<h2>Add sku</h2>
<%= render 'rakuten_products/form' %>